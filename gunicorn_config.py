bind = '0.0.0.0:8000'  # The address to bind to (adjust as needed)
workers = 4  # Number of worker processes (adjust based on your system's capacity)
accesslog = '-'  # Logs access to the console
errorlog = '-'  # Logs errors to the console
loglevel = 'info'  # Level of logging (debug, info, warning, etc.)
timeout = 30  # Timeout for requests in seconds
